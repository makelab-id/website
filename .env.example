PORT=4000
DATABASE_URL=./data/makelab.sqlite

# Admin panel (/admin) — required to log in and to make any change stick.
# Generate a real secret with: openssl rand -hex 32
ADMIN_PASSWORD=change-me
ADMIN_SESSION_SECRET=change-me-too
